#include "vm.h"

void		loop(t_env env)
{
	int		cycle;

	cycle = 0;
	while ()
	{
		while (cycle < CYCLE_TO_DIE)
		{	
			// each process do his thing
				// meaning: exec process if his cycle eq 0 or his cycle--
		}
		check_live(env);
	}
}
